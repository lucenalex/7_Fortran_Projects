program main

	! Este simples programa adiona dois números.

	implicit none

	! Declaração de Variáveis.
	real :: a, b, resultado

	! Delcaração de Executáveis.
	a = 12.0
	b = 15.0
	resultado = a + b

	print *, 'O resultado total eh', resultado

end program main
